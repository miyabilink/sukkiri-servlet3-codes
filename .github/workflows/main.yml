name: release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: create a zip file
        run: ls &&
          zip -r release.zip *章 どこつぶ LICENSE.txt README.md &&
      - uses: actions/checkout@v4
      - name: create a ZIP file
        run: ls &&
          zip -r sukkiri-servlet3-codes.zip *章 どこつぶ LICENSE.txt README.md &&
          unzip -t sukkiri-servlet3-codes.zip
      - name: Create Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          name: Release ${{ github.ref_name }}
          files: sukkiri-servlet3-codes.zip
          draft: false
          prerelease: false
